# CANVAS Project Page

Static project page for **Code-Switching Reveals Language Anchoring in Multilingual LLMs**.

The page is built from the paper bundle `2026_ARR_May___CodeSwitch (8).zip` and uses figures exported from the manuscript.

Preview locally:

```bash
python -m http.server 8000
```
